<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Golden Cloud Server | البوابة الموحدة</title>
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;700;900&display=swap" rel="stylesheet">
    <style>
        :root { --navy: #0d2d4d; --gold: #ffcc00; }
        
        body, html {
            margin: 0; padding: 0; height: 100%;
            overflow: hidden; font-family: 'Tajawal', sans-serif;
            display: flex; justify-content: center; align-items: center;
        }

        /* الفيديو في الخلفية بدون أي عوائق */
        #video-background {
            position: fixed;
            right: 0; bottom: 0;
            min-width: 100%; min-height: 100%;
            width: auto; height: auto;
            z-index: -100;
            object-fit: cover;
        }

        /* طبقة تعتيم شفافة جداً لضمان رؤية الفيديو بوضوح تام */
        .overlay {
            position: fixed;
            top: 0; left: 0;
            width: 100%; height: 100%;
            background: rgba(0, 0, 0, 0.2); 
            z-index: -50;
        }

        /* الحاوية: زجاج صافي ونقي جداً */
        .container { 
            background: transparent; /* لا يوجد لون خلفية */
            padding: 40px; 
            text-align: center; 
            max-width: 400px; 
            width: 85%; 
            z-index: 10;
            /* حد رفيع جداً لتعريف المنطقة فقط */
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 30px;
            /* ضبابية خفيفة جداً تكاد لا تُرى لكسر الإضاءة فقط */
            backdrop-filter: blur(4px);
            -webkit-backdrop-filter: blur(4px);
        }

        h1 { 
            color: white; 
            margin-bottom: 30px; 
            font-size: 32px; 
            font-weight: 900;
            text-shadow: 2px 2px 10px rgba(0,0,0,0.5);
        }

        /* المربعات الثلاثة: واضحة ونظيفة */
        input { 
            width: 100%; 
            padding: 15px; 
            margin-bottom: 15px; 
            border: 1px solid rgba(255,255,255,0.4); 
            border-radius: 12px; 
            box-sizing: border-box; 
            font-family: 'Tajawal'; 
            font-size: 16px;
            background: rgba(255, 255, 255, 0.95); /* المربع الأول والثاني */
            color: var(--navy);
        }

        .btn-main { 
            width: 100%; 
            padding: 16px; 
            background: var(--navy); /* المربع الثالث (الزر) */
            color: var(--gold); 
            border: none; 
            border-radius: 12px; 
            font-weight: 800; 
            font-size: 17px; 
            cursor: pointer; 
            transition: 0.3s; 
        }

        .btn-main:hover { 
            transform: scale(1.03);
            background: #123e66;
        }

        #message { margin-top: 15px; font-size: 14px; font-weight: bold; }
    </style>
</head>
<body>

    <video autoplay muted loop playsinline id="video-background">
        <source src="/0.02.mp4" type="video/mp4">
    </video>

    <div class="overlay"></div>

    <div class="container">
        <div style="font-size: 60px; margin-bottom: 10px; filter: drop-shadow(0 4px 10px rgba(0,0,0,0.5));">☁️</div>
        
        <h1>منصة المهندس محسن الداوودي</h1>

        <form id="loginForm">
            <input type="email" id="email" placeholder="البريد الإلكتروني" required>
            <input type="password" id="password" placeholder="كلمة المرور" required>
            <button type="submit" class="btn-main" id="submitBtn">دخول النظام الموحد 🛡️</button>
        </form>

        <div id="message"></div>
        <div style="margin-top: 40px; font-size: 10px; color: rgba(255,255,255,0.5); letter-spacing: 1px;">GOLDEN CLOUD SYSTEM © 2026</div>
    </div>

    <script>
        document.getElementById('loginForm').addEventListener('submit', async (e) => {
            e.preventDefault();
            const btn = document.getElementById('submitBtn');
            const msg = document.getElementById('message');
            
            btn.innerText = "جاري التحقق... ⏳";
            btn.disabled = true;

            try {
                const response = await fetch('/api/unified-login', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({
                        email: document.getElementById('email').value,
                        password: document.getElementById('password').value
                    })
                });

                const data = await response.json();

                if (data.success) {
                    msg.innerHTML = `<span style="color: #00ff88;">✅ تم التحقق</span>`;
                    window.location.href = data.dashboardUrl;
                } else {
                    msg.innerHTML = `<span style="color: #ff4d4d;">❌ ${data.message}</span>`;
                    btn.innerText = "دخول النظام الموحد 🛡️";
                    btn.disabled = false;
                }
            } catch (err) {
                msg.innerHTML = `<span style="color: #ff4d4d;">⚠️ فشل الاتصال</span>`;
                btn.innerText = "دخول النظام الموحد 🛡️";
                btn.disabled = false;
            }
        });
    </script>
</body>
</html>