<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>تسجيل الشهادة الصحية | Golden Cloud</title>
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;500;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root { --navy: #0b1d2a; --gold: #ffcc00; --bg: #f8fafc; --white: #ffffff; --border: #e2e8f0; }
        body { font-family: 'Tajawal', sans-serif; background: var(--bg); margin: 0; display: flex; justify-content: center; align-items: center; min-height: 100vh; }
        
        .register-card {
            background: var(--white); width: 90%; max-width: 450px; padding: 30px;
            border-radius: 20px; box-shadow: 0 10px 25px rgba(0,0,0,0.1);
            border: 1px solid var(--border);
        }

        .header-logo { text-align: center; margin-bottom: 25px; }
        .header-logo i { font-size: 3rem; color: var(--navy); margin-bottom: 10px; }
        .header-logo h2 { margin: 0; color: var(--navy); font-size: 1.4rem; }
        .header-logo p { color: #64748b; font-size: 0.9rem; margin-top: 5px; }

        label { display: block; margin-bottom: 5px; font-weight: bold; color: var(--navy); font-size: 0.9rem; }
        input { 
            width: 100%; padding: 12px; margin-bottom: 15px; border: 1px solid var(--border);
            border-radius: 10px; box-sizing: border-box; font-family: 'Tajawal'; font-size: 1rem;
        }

        .submit-btn {
            width: 100%; background: var(--navy); color: var(--gold); border: none;
            padding: 15px; border-radius: 10px; font-weight: bold; font-size: 1.1rem;
            cursor: pointer; transition: 0.3s; display: flex; justify-content: center; align-items: center; gap: 10px;
        }
        .submit-btn:disabled { background: #ccc; cursor: not-allowed; }

        .success-msg { display: none; text-align: center; color: #2ed573; }
        .success-msg i { font-size: 4rem; margin-bottom: 15px; }
    </style>
</head>
<body>

<div class="register-card" id="formContainer">
    <div class="header-logo">
        <i class="fas fa-file-signature"></i>
        <h2>تسجيل بيانات الشهادة الصحية</h2>
        <p>يرجى إدخال بياناتك بدقة ليتم إصدار بطاقتك الرقمية</p>
    </div>

    <form id="registrationForm">
        <input type="hidden" id="scopeId" value="<%= scopeId %>">

        <label>الاسم الكامل (كما في الهوية)</label>
        <input type="text" id="name" placeholder="أدخل اسمك الثلاثي" required>

        <label>رقم الهوية / الإقامة</label>
        <input type="text" id="idNum" placeholder="10xxxxxxxx" required>

        <label>رقم الجوال</label>
        <input type="text" id="phone" placeholder="05xxxxxxxx" required>

        <label>رقم شهادة بلدي</label>
        <input type="text" id="baladyNum" placeholder="أدخل رقم الشهادة الصحية">

        <label>تاريخ انتهاء الشهادة</label>
        <input type="date" id="expiryDate" required>

        <button type="submit" id="saveBtn" class="submit-btn">
            إرسال البيانات للتدقيق <i class="fas fa-paper-plane"></i>
        </button>
    </form>
</div>

<div class="register-card success-msg" id="successContainer">
    <i class="fas fa-check-circle"></i>
    <h2>تم استلام بياناتك بنجاح</h2>
    <p>شكراً لك، تم إرسال البيانات إلى نظام الإدارة للمراجعة.</p>
    <button onclick="location.reload()" style="background:none; border:none; color:var(--navy); cursor:pointer; text-decoration:underline; font-family:'Tajawal'">تسجيل شهادة أخرى</button>
</div>

<script>
    document.getElementById('registrationForm').onsubmit = async (e) => {
        e.preventDefault();
        const saveBtn = document.getElementById('saveBtn');
        saveBtn.disabled = true;
        saveBtn.innerText = "جاري الحفظ...";

        const payload = {
            scopeId: document.getElementById('scopeId').value,
            name: document.getElementById('name').value,
            idNum: document.getElementById('idNum').value,
            phone: document.getElementById('phone').value,
            baladyNum: document.getElementById('baladyNum').value,
            expiryDate: document.getElementById('expiryDate').value
        };

        try {
            const response = await fetch('/health/add', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(payload)
            });

            const result = await response.json();

            if (response.ok && result.success) {
                document.getElementById('formContainer').style.display = 'none';
                document.getElementById('successContainer').style.display = 'block';
            } else {
                alert(result.message || "حدث خطأ أثناء الحفظ");
                saveBtn.disabled = false;
                saveBtn.innerText = "إرسال البيانات للتدقيق";
            }
        } catch (err) {
            alert("فشل الاتصال بالسيرفر، تأكد من الإنترنت.");
            saveBtn.disabled = false;
        }
    };
</script>

</body>
</html>