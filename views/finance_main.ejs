<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <title>الإدارة المالية | شركة الكوارتز</title>
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;700;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --gold: #ffcc00;
            --navy: #0a192f;
            --success: #2ecc71;
            --danger: #e74c3c;
            --bg-light: #f4f7f6;
        }
        body { font-family: 'Tajawal', sans-serif; margin: 0; display: flex; background: var(--bg-light); }
        .sidebar { width: 260px; background: var(--navy); height: 100vh; color: white; padding: 20px; position: fixed; }
        .sidebar-logo { text-align: center; padding-bottom: 30px; border-bottom: 1px solid rgba(255,255,255,0.1); }
        .menu-item { 
            display: block; padding: 15px; color: #a8b2d1; text-decoration: none; 
            transition: 0.3s; border-radius: 8px; margin-bottom: 5px;
        }
        .menu-item:hover, .menu-item.active { background: rgba(255,204,0,0.1); color: var(--gold); }
        .main-content { margin-right: 260px; padding: 40px; width: calc(100% - 260px); }
        .stat-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 20px; margin-bottom: 30px; }
        .stat-card { background: white; padding: 20px; border-radius: 15px; box-shadow: 0 4px 15px rgba(0,0,0,0.05); border-right: 5px solid var(--gold); }
        .stat-card h4 { margin: 0; color: #666; font-size: 0.9rem; }
        .stat-card .value { font-size: 1.5rem; font-weight: 900; margin-top: 10px; color: var(--navy); }
        .table-container { background: white; padding: 25px; border-radius: 15px; box-shadow: 0 4px 15px rgba(0,0,0,0.05); }
        table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        th { text-align: right; padding: 12px; border-bottom: 2px solid #eee; color: #888; }
        td { padding: 15px 12px; border-bottom: 1px solid #eee; }
        .modal { display: none; position: fixed; z-index: 2000; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.6); }
        .modal-content { background: white; width: 500px; margin: 50px auto; padding: 30px; border-radius: 15px; }
    </style>
</head>
<body>

    <div class="sidebar">
        <div class="sidebar-logo">
            <i class="fas fa-gem fa-2x" style="color: var(--gold);"></i>
            <h3>الكوارتز المالية</h3>
        </div>
        <nav>
            <a href="#" class="menu-item active"><i class="fas fa-chart-pie"></i> لوحة التحكم</a>
            <a href="#" class="menu-item"><i class="fas fa-vault"></i> الصناديق والبنك</a>
            <a href="#" class="menu-item"><i class="fas fa-truck"></i> حسابات الموردين</a>
            <a href="#" class="menu-item"><i class="fas fa-user-tie"></i> حسابات العملاء</a>
            <a href="#" class="menu-item"><i class="fas fa-file-invoice"></i> التقارير الضريبية</a>
            
            <div style="margin-top: 50px;">
                <a href="/panel/main-panel?scope=<%= scopeId %>" class="menu-item" style="color: var(--gold); border: 1px solid var(--gold);">
                    <i class="fas fa-arrow-right"></i> العودة للوحة الأم
                </a>
            </div>
        </nav>
    </div>

    <main class="main-content">
        <header style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 30px;">
            <h2>ملخص المركز المالي <small style="font-size: 0.9rem; color: #666;">(نطاق: <%= scopeId %>)</small></h2>
            <div>
                <button onclick="openModal('قبض')" style="background: var(--success); color: white; border: none; padding: 10px 20px; border-radius: 8px; cursor: pointer; font-weight: bold;">
                    <i class="fas fa-plus"></i> سند قبض
                </button>
                <button onclick="openModal('صرف')" style="background: var(--danger); color: white; border: none; padding: 10px 20px; border-radius: 8px; cursor: pointer; font-weight: bold;">
                    <i class="fas fa-minus"></i> سند صرف
                </button>
            </div>
        </header>

        <div class="stat-grid">
            <div class="stat-card">
                <h4><i class="fas fa-university"></i> رصيد البنك</h4>
                <div class="value">0.00 <small>ر.س</small></div>
            </div>
            <div class="stat-card">
                <h4><i class="fas fa-money-bill-wave"></i> صندوق الكاش (إيراد)</h4>
                <div class="value"><%= income.toLocaleString() %> <small>ر.س</small></div>
            </div>
            <div class="stat-card" style="border-right-color: var(--danger);">
                <h4><i class="fas fa-arrow-trend-down"></i> إجمالي المصاريف</h4>
                <div class="value"><%= expenses.toLocaleString() %> <small>ر.س</small></div>
            </div>
            <div class="stat-card" style="border-right-color: var(--success);">
                <h4><i class="fas fa-scale-balanced"></i> صافي الربح</h4>
                <div class="value"><%= balance.toLocaleString() %> <small>ر.س</small></div>
            </div>
        </div>

        <div class="table-container">
            <h3>أحدث 10 عمليات مالية</h3>
            <table>
                <thead>
                    <tr>
                        <th>التاريخ</th>
                        <th>النوع</th>
                        <th>المبلغ</th>
                        <th>البيان</th>
                    </tr>
                </thead>
                <tbody>
                    <% if(transactions && transactions.length > 0) { %>
                        <% transactions.forEach(t => { %>
                            <tr>
                                <td><%= new Date(t.date).toLocaleDateString('ar-SA') %></td>
                                <td style="color: <%= t.type === 'قبض' ? 'green' : 'red' %>; font-weight: bold;"><%= t.type %></td>
                                <td><%= t.amount.toLocaleString() %> ر.س</td>
                                <td><%= t.description %></td>
                            </tr>
                        <% }) %>
                    <% } else { %>
                        <tr><td colspan="4" style="text-align: center;">لا توجد عمليات حالياً</td></tr>
                    <% } %>
                </tbody>
            </table>
        </div>
    </main>

    <div id="transModal" class="modal">
        <div class="modal-content">
            <h3 id="modalTitle">إضافة عملية</h3>
            <form action="/finance/add-transaction" method="POST">
                <input type="hidden" name="scopeId" value="<%= scopeId %>">
                <input type="hidden" name="type" id="transType">
                <div style="margin-bottom: 15px;">
                    <label>المبلغ</label>
                    <input type="number" name="amount" required style="width:100%; padding:10px; margin-top:5px; border:1px solid #ddd; border-radius:5px;">
                </div>
                <div style="margin-bottom: 15px;">
                    <label>البيان</label>
                    <textarea name="description" required style="width:100%; padding:10px; margin-top:5px; border:1px solid #ddd; border-radius:5px;"></textarea>
                </div>
                <button type="submit" style="width:100%; background: var(--navy); color: white; padding: 12px; border: none; border-radius: 8px; cursor: pointer;">ترحيل السند</button>
                <button type="button" onclick="closeModal()" style="width:100%; background: none; border: none; color: gray; margin-top: 10px; cursor: pointer;">إلغاء</button>
            </form>
        </div>
    </div>

    <script>
        function openModal(type) {
            document.getElementById('transModal').style.display = 'block';
            document.getElementById('transType').value = type;
            document.getElementById('modalTitle').innerText = 'إضافة سند ' + type;
        }
        function closeModal() {
            document.getElementById('transModal').style.display = 'none';
        }
    </script>
</body>
</html>