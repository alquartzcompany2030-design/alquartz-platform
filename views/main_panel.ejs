<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <title>اللوحة الأم | شركة الكوارتز</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;700;900&display=swap" rel="stylesheet">
    <style>
        :root { 
            --gold: #ffcc00; 
            --danger: #e74c3c;
            --info-blue: #3498db;
            --success-green: #2ecc71;
        }
        
        body { 
            font-family: 'Tajawal', sans-serif; 
            background: url('/0.022.png') no-repeat center center fixed; 
            background-size: cover;
            margin: 0; display: flex; flex-direction: column; min-height: 100vh;
            color: white; 
        }

        header { 
            background: transparent; 
            padding: 20px 40px; 
            display: flex; 
            justify-content: space-between; 
            align-items: center; 
            position: sticky; top: 0; z-index: 1000;
        }

        .logo-section { display: flex; flex-direction: column; gap: 5px; }
        .user-welcome { font-size: 1.1rem; font-weight: bold; color: white; text-shadow: 0 2px 4px rgba(0,0,0,0.5); margin: 0; }

        .scope-badge { 
            background: var(--gold); 
            padding: 5px 20px; border-radius: 8px; 
            color: #000; 
            font-size: 0.9rem; font-weight: 900;
            display: inline-flex; align-items: center; gap: 8px;
            box-shadow: 0 4px 10px rgba(0,0,0,0.2);
        }

        .header-tools { display: flex; align-items: center; gap: 15px; }

        .btn-tool {
            text-decoration: none; display: flex; align-items: center; gap: 8px;
            padding: 10px 20px; border-radius: 12px; color: white;
            font-size: 0.9rem; font-weight: bold; transition: 0.3s;
            border: none; cursor: pointer;
        }
        .btn-users { background: var(--info-blue); }
        .btn-perms { background: var(--success-green); }
        .btn-logout { background: var(--danger); }
        
        .btn-tool:hover { transform: translateY(-2px); filter: brightness(1.1); box-shadow: 0 5px 15px rgba(0,0,0,0.3); }

        .main-panel-container { flex: 1; padding: 40px 20px; display: flex; justify-content: center; align-items: center; }
        .grid-layout { 
            display: grid; grid-template-columns: repeat(auto-fit, minmax(320px, 1fr)); 
            gap: 35px; width: 100%; max-width: 1300px; 
        }
        
        .panel-card { 
            background: transparent; border-radius: 35px; padding: 45px 25px; 
            text-align: center; text-decoration: none; color: white; 
            transition: all 0.5s cubic-bezier(0.25, 1, 0.5, 1); 
            border: 1px solid rgba(255, 255, 255, 0.2); 
            display: flex; flex-direction: column; align-items: center; position: relative;
        }

        .panel-card:hover { 
            transform: translateY(-10px); 
            background: rgba(255, 255, 255, 0.05); 
            backdrop-filter: blur(8px); -webkit-backdrop-filter: blur(8px);
            border-color: var(--gold);
            box-shadow: 0 20px 50px rgba(0, 0, 0, 0.5);
        }

        .panel-card h3 { text-shadow: 0 4px 10px rgba(0,0,0,0.8); margin: 20px 0 10px; font-size: 1.7rem; font-weight: 900; }
        .panel-card p { text-shadow: 0 2px 4px rgba(0,0,0,0.8); font-size: 1rem; color: #eee; line-height: 1.6; }

        .panel-card i { font-size: 4.5rem; filter: drop-shadow(0 5px 15px rgba(0,0,0,0.7)); margin-bottom: 10px; transition: 0.4s; }
        .panel-card:hover i { transform: scale(1.1); }

        .status-badge { 
            margin-top: 25px; background: rgba(0,0,0,0.5); padding: 8px 22px; 
            border-radius: 50px; font-size: 0.85rem; border: 1px solid rgba(255,255,255,0.2); 
            display: flex; align-items: center; gap: 10px;
        }
        .status-dot { width: 10px; height: 10px; border-radius: 50%; }
        .online { background: #00ff88; }
        .in-progress { background: #f39c12; }

        footer { padding: 20px; text-align: center; color: white; font-weight: bold; opacity: 0.8; }
        .locked-card { cursor: default; }
    </style>
</head>
<body>

<header>
    <div class="logo-section">
        <h2 class="user-welcome" id="welcomeName">مرحباً، أبو حمزة</h2>
        <div class="scope-badge">
            <i class="fas fa-building"></i> 
            <span id="scopeText">المتزن لخدمات السيارات</span>
        </div>
    </div>

    <div class="header-tools">
        <a href="#" id="linkUsers" class="btn-tool btn-users">
            <i class="fas fa-users"></i> إدارة المستخدمين
        </a>
        <a href="#" id="linkPerms" class="btn-tool btn-perms">
            <i class="fas fa-shield-halved"></i> الصلاحيات
        </a>
        <button class="btn-tool btn-logout" onclick="logout()">
            <i class="fas fa-power-off"></i> خروج
        </button>
    </div>
</header>

<div class="main-panel-container">
    <div class="grid-layout">
        <a href="#" id="linkHR" class="panel-card">
            <i class="fas fa-users-viewfinder" style="color: #4db8ff;"></i>
            <h3>الموارد البشرية</h3>
            <p>إدارة مصفوفة الرصد، التوظيف، وسجلات المنشأة.</p>
            <div class="status-badge"><span class="status-dot online"></span> متصل</div>
        </a>

        <a href="javascript:void(0)" class="panel-card locked-card">
            <i class="fas fa-chart-line" style="color: #00ff88;"></i>
            <h3>الحسابات المالية</h3>
            <p>مراقبة التدفقات النقدية والموردين والعملاء.</p>
            <div class="status-badge"><span class="status-dot in-progress"></span> قيد التطوير</div>
        </a>

        <a href="javascript:void(0)" class="panel-card locked-card">
            <i class="fas fa-warehouse" style="color: #ff9f43;"></i>
            <h3>إدارة المخازن</h3>
            <p>تتبع المواد الخام، المنتجات، والأصول والمخزون.</p>
            <div class="status-badge"><span class="status-dot in-progress"></span> قيد التطوير</div>
        </a>

        <a href="javascript:void(0)" class="panel-card locked-card">
            <i class="fas fa-file-invoice-dollar" style="color: #feca57;"></i>
            <h3>المبيعات والعقود</h3>
            <p>إدارة عروض الأسعار وفواتير المبيعات وعقود العملاء.</p>
            <div class="status-badge"><span class="status-dot in-progress"></span> قيد التطوير</div>
        </a>

        <a href="javascript:void(0)" class="panel-card locked-card">
            <i class="fas fa-shopping-cart" style="color: #ff9ff3;"></i>
            <h3>المتجر الإلكتروني</h3>
            <p>إدارة الطلبات أونلاين وبوابة الدفع الإلكترونية.</p>
            <div class="status-badge"><span class="status-dot in-progress"></span> قيد التطوير</div>
        </a>

        <a href="javascript:void(0)" class="panel-card locked-card">
            <i class="fas fa-laptop-code" style="color: #54a0ff;"></i>
            <h3>الخدمات التقنية</h3>
            <p>إدارة الدعم الفني والحلول السحابية للمنشأة.</p>
            <div class="status-badge"><span class="status-dot in-progress"></span> قيد التطوير</div>
        </a>
    </div>
</div>

<footer>
    &copy; 2026 شركة الكوارتز | تطوير أبو حمزة
</footer>

<script>
    // وظيفة ذكية لاستخراج الـ ScopeID من الرابط وتثبيته في الأزرار
    function updateLinks() {
        const urlParams = new URLSearchParams(window.location.search);
        const currentScope = urlParams.get('scope');

        if (currentScope) {
            // تحديث روابط الهيدر
            document.getElementById('linkUsers').href = `/manager/users-management?scope=${currentScope}`;
            document.getElementById('linkPerms').href = `/manager/permissions?scope=${currentScope}`;
            
            // تحديث رابط الموارد البشرية (الداشبورد) لضمان عدم الخروج
            document.getElementById('linkHR').href = `/manager/dashboard?scope=${currentScope}`;
        } else {
            // حالة احتياطية إذا لم يوجد Scope في الرابط
            document.getElementById('linkHR').href = '/manager/dashboard';
        }
    }

    window.onload = updateLinks;

    function logout() {
        if(confirm("هل تود الخروج من النظام؟")) window.location.href = '/';
    }
</script>
</body>
</html>